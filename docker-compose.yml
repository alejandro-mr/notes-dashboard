version: "3.5"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - ./yarn.lock:/app/yarn.lock
      - ./package.json:/app/package.json
    command: yarn start
