type Note {
  id: ID!,
  title: String,
  content: String!,
  position: Position!,
  width: Int!
  height: Int!
}

type Position {
  x: Int!,
  y: Int!,
  z: Int!
}

type Query {
  notes: [Note]
  note(id: ID!): Note
}

schema {
  query: Query
}